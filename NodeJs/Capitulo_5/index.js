let redondeo = Math.floor(((0.1+0.2)*100)/100);
console.log(redondeo)

//to.fixed(x) devuelve la cantifaf decimales;limita la cantidad
// to precision, devuelve las cifras significativas, ambas regresan un string y no un numero

// .valueOf() Obtener valores numericos a partir de literaels
// b = new Number(x);
// parseInt(parte entera) o Number() convierten string a numero .toString viceversa
//Hexadecimales(Base 16) se convierten tambien con parseint
//Number.EPSILON